// Finance page now just renders Banking directly
// Vault has been moved to its own page in the Business section
import Banking from './Banking';

export default function Finance() {
  return <Banking />;
}
